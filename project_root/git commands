# Initialize Git (if not already done)
git init

# Add all files
git add .

# Commit your changes
git commit -m "Initial backend with voice and image input"

# Connect to your GitHub repo (replace the link with your actual repo URL)
git remote add origin https://github.com/logitechsabarni/AI-chatbot.git

# Push the code
git branch -M main
git push -u origin main
